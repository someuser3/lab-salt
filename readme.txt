In this directory are stored salt states. Some are work in progress, others are pretty much completed.

To make it work I moved /srv/salt directory to /root/projects/saltstates/salt and created a symlink:

ln -s /root/projects/saltstates/salt /srv/salt

